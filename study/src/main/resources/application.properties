# properties
#db source url ->db와 어떤 주소로 연결할 것인지 설정하는 부분
spring.datasource.url=jdbc:mysql://localhost:3306/study?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
#localhost(컴퓨터에 db 존재)/schema?~옵션

# db response name
spring.datasource.username=root

# db response password
spring.datasource.password=qwerty

#jpa가 실행시킨 sql문에 대해서 보겠다
spring.jpa.show-sql=true
